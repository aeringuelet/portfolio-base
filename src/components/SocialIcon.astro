---
import { Image } from 'astro:assets';

interface Props {
    icon: ImageMetadata;
    alt: string;
    class?: string;
}

const { alt, icon, class: className } = Astro.props;
---

<Image src={icon} alt={alt} class={className} />
