---
interface Props {
    sectionName: string;
    class?: string;
}

const { sectionName, class: className, ...rest } = Astro.props;
---

<section class={`${className ? className : ''}`} id={sectionName} {...rest}>
    <slot />
</section>
